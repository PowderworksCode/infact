[facts]
parser-paths = ["../entl/parser-packs"]

[errors]
allowed-in = [
    # Fixture source is input to an analyzer, not code that runs. The discards
    # in it are the thing under test.
    "**/tests/fixtures/**",
    # Test support that walks a downloaded corpus.
    "**/tests/**",
]
